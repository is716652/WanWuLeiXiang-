# 用户协议和隐私政策功能说明

## ✅ 功能概述

为万物类象APP添加了完整的用户协议和隐私政策功能，符合应用市场上架要求。

## 📋 实现内容

### 1️⃣ 页面修正

**修改文件：**
- `UserAgreement.ets` - 用户协议页面
- `PrivacyPolicy.ets` - 隐私政策页面

**修正内容：**
- ✅ 将"象中六壬APP" → "万物类象APP"
- ✅ 修正导航引用，使用 `router` 替代不存在的 `Navigation` 工具类
- ✅ 保持原有完整的法律文本内容

### 2️⃣ 首次访问弹窗

**实现位置：** `Index.ets` 首页

**功能特性：**
- 📱 **首次访问自动弹出** - 使用 `preferences` 持久化存储
- 🔒 **遮罩层设计** - 半透明黑色背景，聚焦协议内容
- 📄 **协议链接** - 可点击查看完整协议和隐私政策
- ✅ **同意机制** - 点击"同意并继续"后保存状态
- ❌ **拒绝处理** - 点击"不同意并退出"返回上一页

**首次访问判断逻辑：**
```typescript
async checkFirstVisit(): Promise<void> {
  const dataPreferences = await preferences.getPreferences(getContext(this), 'appSettings');
  const hasAgreed = await dataPreferences.get('hasAgreedToTerms', false);
  
  if (!hasAgreed) {
    this.showAgreementDialog = true; // 显示弹窗
  }
}
```

**同意后保存：**
```typescript
async agreeToTerms(): Promise<void> {
  const dataPreferences = await preferences.getPreferences(getContext(this), 'appSettings');
  await dataPreferences.put('hasAgreedToTerms', true);
  await dataPreferences.flush();
  this.showAgreementDialog = false;
}
```

### 3️⃣ 底部查看入口

**位置：** 首页底部，"点击卡片进入查询"下方

**设计：**
```
    点击卡片进入查询
    
  用户协议  |  隐私政策
```

**样式特点：**
- 半透明白色文字（opacity: 0.6）
- 可点击跳转到对应页面
- 分隔符"|"区分两个链接
- 与整体暗色背景和谐统一

## 🎨 弹窗设计

### 视觉效果
- **遮罩层**：`rgba(0, 0, 0, 0.5)` 半透明黑色
- **卡片背景**：白色 `#FFFFFF`
- **卡片宽度**：85% 屏幕宽度
- **圆角**：16px
- **阴影**：深度阴影效果，增强层次感

### 内容结构
```
┌─────────────────────────────┐
│  用户协议与隐私政策          │
├─────────────────────────────┤
│  欢迎使用万物类象APP！      │
│                             │
│  请您仔细阅读并同意：       │
│  《用户协议》和《隐私政策》 │
│  （可点击查看）             │
│                             │
│  当前版本不收集您的个人信息 │
├─────────────────────────────┤
│ [不同意并退出] [同意并继续] │
└─────────────────────────────┘
```

### 交互逻辑
1. **首次打开APP** → 自动显示弹窗
2. **点击协议链接** → 跳转到详细页面查看
3. **点击"同意并继续"** → 保存状态 + 关闭弹窗
4. **点击"不同意并退出"** → 返回（无法使用APP）
5. **再次打开APP** → 不再显示弹窗

## 📱 用户体验流程

### 首次访问
```
打开APP
   ↓
自动弹出协议弹窗
   ↓
用户阅读/点击查看详情
   ↓
点击"同意并继续"
   ↓
保存同意状态（preferences）
   ↓
关闭弹窗，进入首页
```

### 后续访问
```
打开APP
   ↓
检查preferences中的hasAgreedToTerms
   ↓
已同意 → 直接显示首页
   ↓
用户可通过底部入口随时查看协议
```

## 🔧 技术实现

### 文件修改

**1. Index.ets（首页）**
- 导入 `preferences` 模块
- 添加 `showAgreementDialog` 状态变量
- 实现 `checkFirstVisit()` 检查方法
- 实现 `agreeToTerms()` 保存方法
- 添加协议弹窗UI
- 添加底部入口链接

**2. UserAgreement.ets（用户协议）**
- 修改导入：`Navigation` → `router`
- 修改APP名称：象中六壬 → 万物类象
- 修正跳转方法

**3. PrivacyPolicy.ets（隐私政策）**
- 修改导入：`Navigation` → `router`
- 修改APP名称：象中六壬 → 万物类象

**4. main_pages.json（路由配置）**
- 添加：`pages/Legal/UserAgreement`
- 添加：`pages/Legal/PrivacyPolicy`

### 数据持久化

使用HarmonyOS的 `preferences` API：
- **存储位置**：`appSettings`
- **键名**：`hasAgreedToTerms`
- **值类型**：`boolean`
- **默认值**：`false`

### 符合ArkTS规范
- ✅ 异步方法使用 `async/await`
- ✅ 明确的类型定义 `Promise<void>`
- ✅ 正确的上下文获取 `getContext(this)`
- ✅ 状态管理使用 `@State`
- ✅ 零编译错误

## 📄 法律文本内容

### 用户协议包含：
- 总则
- 服务内容
- 用户行为规范
- 知识产权
- 隐私保护（链接到隐私政策）
- 免责声明与责任限制
- 变更与终止
- 联系方式

### 隐私政策包含：
- 导言
- 信息收集说明（当前不收集）
- 权限调用说明
- 静默状态说明
- 第三方SDK说明（当前无）
- 信息存储
- 信息安全
- 信息使用
- 信息共享
- 用户权利
- 变更说明
- 未成年人保护

## ✨ 特色亮点

### 1. 合规性
- 📋 完整的法律文本
- 🔒 明确的隐私说明
- ✅ 符合应用市场上架要求
- 📱 遵循HarmonyOS规范

### 2. 用户友好
- 🎯 首次访问自动提示
- 📖 可随时查看详情
- 🔗 协议内容可点击跳转
- 💬 简洁清晰的表述

### 3. 技术规范
- 🏗️ 符合ArkTS规范
- 💾 持久化存储状态
- 🔄 正确的异步处理
- 🎨 良好的UI设计

### 4. 隐私保护
- 🔐 当前版本不收集个人信息
- 🚫 无第三方SDK
- 📱 无需账号即可使用
- 🔒 数据本地存储

## 🚀 使用场景

### 场景1：新用户首次安装
1. 打开APP
2. 自动弹出协议弹窗
3. 阅读协议内容或点击查看详情
4. 点击"同意并继续"
5. 开始使用APP

### 场景2：老用户日常使用
1. 打开APP
2. 直接进入首页（无弹窗）
3. 如需查看协议，点击底部链接

### 场景3：查看协议详情
1. 首页底部点击"用户协议"或"隐私政策"
2. 跳转到详细页面
3. 滚动查看完整内容
4. 点击"返回"回到首页

### 场景4：从弹窗跳转查看
1. 首次访问弹窗中
2. 点击蓝色《用户协议》或《隐私政策》链接
3. 跳转到详细页面查看
4. 返回后弹窗仍在
5. 阅读完毕后点击"同意并继续"

## 📊 编译状态

```
BUILD SUCCESSFUL in 15s
```

✅ **零错误、零警告** - 完全通过ArkTS编译检查！

## 🎯 符合要求

✅ **修正APP名称** - 象中六壬 → 万物类象  
✅ **首次弹窗** - 自动显示，同意后不再显示  
✅ **底部入口** - 可随时查看协议  
✅ **导航修正** - 使用正确的router API  
✅ **持久化存储** - 使用preferences保存状态  

---

**完成时间**：2025-11-11  
**编译版本**：HarmonyOS ArkTS  
**文件数量**：3个页面文件 + 1个配置文件
