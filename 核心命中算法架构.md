# 核心命中算法架构

> **万物类象 - 百晓生AI智能体命中算法技术文档**  
> **版本**: 1.0.0  
> **更新日期**: 2025-11-22  
> **分支**: agent

---

## 📋 目录

1. [算法概述](#算法概述)
2. [知识库检索算法](#知识库检索算法)
3. [组合解析算法](#组合解析算法)
4. [规则驱动计算](#规则驱动计算)
5. [性能优化策略](#性能优化策略)
6. [使用示例](#使用示例)

---

## 算法概述

百晓生AI智能体采用**三层递进命中策略**，实现高效的本地知识库检索与智能组合解析：

```
用户输入 → 分词预处理 → 索引命中 → 扫描匹配 → 二字分词回退 → 组合解析 → 规则驱动计算 → 结果展示
```

### 核心特点

- ✅ **本地优先** - 零网络依赖，毫秒级响应
- ✅ **中文优化** - 二字分词提升自然语句命中率
- ✅ **轻量索引** - Map<string, string[]> O(1)查找
- ✅ **规则驱动** - JSON配置 + 动态映射计算
- ✅ **异常保护** - try-catch隔离，友好降级
- ✅ **内存安全** - 按需加载，避免手机内存溢出

---

## 知识库检索算法

### 数据结构

```typescript
export interface KBItem {
  id: string;        // 文件名，如 "dizhi_zi.json"
  title: string;     // 标题
  type: string;      // 'json' | 'md'
  tags: string[];    // 标签数组
  content: string;   // 内容
}

class KnowledgeBase {
  private items: KBItem[] = [];                    // 全部知识条目
  private tokenIndex: Map<string, string[]>;        // 轻量索引
}
```

### 三层递进检索策略

#### 第一层：分词预处理

```typescript
search(keyword: string): KBItem[] {
  const key = keyword.trim();
  if (key === '') { return []; }  // 空输入直接返回
  
  // 规范化：将常见连接符替换为空格
  let normalized = key
    .replace('，', ' ')
    .replace('、', ' ')
    .replace('与', ' ')
    .replace('和', ' ')
    .replace(',', ' ');
  
  const tokens = normalized.split(' ');  // ["河图", "洛书"]
}
```

**示例**：
- 输入："河图 与 洛书" → tokens: `["河图", "洛书"]`
- 输入："子丑寅卯" → tokens: `["子丑寅卯"]`（无分隔符，整体保留）

#### 第二层：索引命中（最快，O(1)）

```typescript
// 从预建索引中查找
const idSet: Set<string> = new Set();
for (let t = 0; t < tokens.length; t++) {
  const token = tokens[t];
  const ids = this.tokenIndex.get(token);  // O(1) Map查找
  if (ids) {
    for (let k = 0; k < ids.length; k++) {
      idSet.add(ids[k]);  // 收集命中的 item id
    }
  }
}

// 从 items 中提取命中结果
if (idSet.size > 0) {
  for (let i = 0; i < this.items.length; i++) {
    if (idSet.has(this.items[i].id)) {
      results.push(this.items[i]);
    }
  }
}
```

**索引构建机制**：

```typescript
private indexItem(item: KBItem): void {
  const tokens: string[] = [];
  
  // 1. 标题直接入索引
  tokens.push(item.title);
  
  // 2. 标签入索引
  for (let i = 0; i < item.tags.length; i++) {
    tokens.push(item.tags[i]);
  }
  
  // 3. 标题的二字分词入索引（中文优化）
  // "河图洛书" → ["河图", "图洛", "洛书"]
  const len = item.title.length;
  for (let i = 0; i < len - 1; i++) {
    const gram = item.title.substring(i, i + 2);
    tokens.push(gram);
  }
  
  // 将所有 token 映射到 item.id
  for (let t = 0; t < tokens.length; t++) {
    const tk = tokens[t];
    if (tk === '') { continue; }
    const exists = this.tokenIndex.get(tk);
    if (exists) {
      if (!exists.includes(item.id)) {
        exists.push(item.id);
      }
    } else {
      this.tokenIndex.set(tk, [item.id]);
    }
  }
}
```

**自动标签生成**（提升命中率）：

```typescript
// 加载 dizhi_zi.json 时自动加标签
if (fileName.indexOf('dizhi_') === 0) {
  tags.push('地支', '十二地支', '干支');
  const han = getDizhiHan(fileName);  // 提取 "子"
  if (han !== '') { tags.push(han); }
}

// 加载 tiangan_jia.json 时
if (fileName.indexOf('tiangan_') === 0) {
  tags.push('天干', '十天干', '干支');
  const han = getTianganHan(fileName);  // 提取 "甲"
  if (han !== '') { tags.push(han); }
}
```

#### 第三层：扫描匹配（索引未命中时）

```typescript
if (results.length === 0) {
  for (let i = 0; i < this.items.length; i++) {
    const it = this.items[i];
    let matched = false;
    
    for (let t = 0; t < tokens.length; t++) {
      const token = tokens[t];
      if (token === '') { continue; }
      
      // 检查 token 是否在 title/content/tags 中
      const inTitle = it.title.indexOf(token) >= 0;
      const inContent = it.content.indexOf(token) >= 0;
      let inTags = false;
      for (let j = 0; j < it.tags.length; j++) {
        if (it.tags[j].indexOf(token) >= 0) {
          inTags = true;
          break;
        }
      }
      
      if (inTitle || inContent || inTags) {
        matched = true;
        break;
      }
    }
    
    if (matched) {
      results.push(it);
    }
  }
}
```

#### 第四层：中文二字分词回退（仍未命中）

```typescript
if (results.length === 0) {
  const grams: string[] = [];
  const len = key.length;
  
  // 生成二字分词："洛书是啥" → ["洛书", "书是", "是啥"]
  for (let i = 0; i < len - 1; i++) {
    const gram = key.substring(i, i + 2);
    // 跳过包含标点的gram
    if (gram.indexOf(' ') >= 0 || gram.indexOf('，') >= 0) {
      continue;
    }
    grams.push(gram);
  }
  
  // 用二字分词再次扫描匹配
  for (let i = 0; i < this.items.length; i++) {
    const it = this.items[i];
    let hit = false;
    for (let g = 0; g < grams.length; g++) {
      const gram = grams[g];
      if (it.title.indexOf(gram) >= 0 || it.content.indexOf(gram) >= 0) {
        hit = true;
        break;
      }
      for (let j = 0; j < it.tags.length; j++) {
        if (it.tags[j].indexOf(gram) >= 0) {
          hit = true;
          break;
        }
      }
      if (hit) { break; }
    }
    if (hit) {
      results.push(it);
    }
  }
}
```

---

## 组合解析算法

### 输入识别流程

```typescript
analyze(query: string, kb: KnowledgeBase): KBItem {
  const key = query.trim();
  const summaryLines: string[] = [];
  summaryLines.push('【组合解析报告】');
  this.loadRules(kb);  // 懒加载规则
  
  // 第一步：分词
  const tokens = this.splitTokens(key);
  
  // 第二步：符号识别（从预定义字典匹配）
  const dizhiAll = ['子','丑','寅','卯','辰','巳','午','未','申','酉','戌','亥'];
  const ganAll = ['甲','乙','丙','丁','戊','己','庚','辛','壬','癸'];
  const baguaAll = ['乾','坤','震','巽','坎','离','艮','兑'];
  
  const zhiFound = this.pickFrom(tokens, dizhiAll);  // ["子","丑","寅","卯"]
  const ganFound = this.pickFrom(tokens, ganAll);
  const baguaFound = this.pickFrom(tokens, baguaAll);
}
```

### 符号提取算法（pickFrom）

```typescript
private pickFrom(tokens: string[], dict: string[]): string[] {
  const res: string[] = [];
  for (let i = 0; i < dict.length; i++) {
    const d = dict[i];
    for (let j = 0; j < tokens.length; j++) {
      if (tokens[j].indexOf(d) >= 0) {  // "子丑" 包含 "子"
        // 去重
        let exists = false;
        for (let k = 0; k < res.length; k++) {
          if (res[k] === d) { exists = true; break; }
        }
        if (!exists) { res.push(d); }
      }
    }
  }
  return res;
}
```

### 关系计算（基于固定规则）

#### 地支六合

```typescript
private calcDizhiHe(zhi: string[]): string[] {
  const pairs = [['子','丑'],['寅','亥'],['卯','戌'],
                 ['辰','酉'],['巳','申'],['午','未']];
  return this.pickPairs(zhi, pairs, '');
}

private pickPairs(input: string[], pairs: Array<string[]>, joiner: string): string[] {
  const res = [];
  for (let i = 0; i < pairs.length; i++) {
    const p = pairs[i];
    if (this.containsAll(input, p)) {  // ["子","丑"] 都在 input 中
      const label = joiner === '' ? (p[0] + p[1]) : (p[0] + joiner + p[1]);
      res.push(label);  // "子丑"
    }
  }
  return res;
}

private containsAll(have: string[], need: string[]): boolean {
  for (let i = 0; i < need.length; i++) {
    let found = false;
    for (let j = 0; j < have.length; j++) {
      if (have[j] === need[i]) { found = true; break; }
    }
    if (!found) { return false; }
  }
  return true;
}
```

#### 地支六冲

```typescript
private calcDizhiChong(zhi: string[]): string[] {
  const pairs = [['子','午'],['丑','未'],['寅','申'],
                 ['卯','酉'],['辰','戌'],['巳','亥']];
  return this.pickPairs(zhi, pairs, '');
}
```

#### 地支三合

```typescript
private calcDizhiSanHe(zhi: string[]): string[] {
  const groups = [['申','子','辰'],['亥','卯','未'],
                  ['寅','午','戌'],['巳','酉','丑']];
  const res = [];
  for (let i = 0; i < groups.length; i++) {
    const g = groups[i];
    if (this.containsAll(zhi, g)) {  // 三个都在
      res.push(g.join(''));  // "申子辰"
    }
  }
  return res;
}
```

#### 天干五合

```typescript
private calcTianganWuHe(gan: string[]): string[] {
  const pairs = [['甲','己'],['乙','庚'],['丙','辛'],
                 ['丁','壬'],['戊','癸']];
  return this.pickPairs(gan, pairs, '');
}
```

---

## 规则驱动计算

### 规则文件结构（rules/combination_rules.json）

```json
{
  "version": "1.0.0",
  "cards": [
    {
      "id": "dizhi",
      "title": "十二地支",
      "matchTokens": ["子","丑","寅"...]
    },
    {
      "id": "tiangan",
      "title": "十天干",
      "matchTokens": ["甲","乙","丙"...]
    }
  ],
  "combos": [
    {
      "id": "dizhi_relations",
      "title": "地支组合关系",
      "fn": "dizhi_relations",
      "use": ["cards.dizhi"]
    }
  ],
  "maps": {
    "gua_transform": [
      { "name": "乾", "wrong": "坤", "overturn": "乾", "mutual": ["乾"] },
      { "name": "坎", "wrong": "离", "overturn": "坎", "mutual": ["坎"] }
    ],
    "calendar": {
      "year_ganzhi": [
        { "key": "2024", "value": "甲辰" },
        { "key": "2025", "value": "乙巳" }
      ],
      "ganzhi_nayin": [
        { "key": "甲辰", "value": "覆灯火" },
        { "key": "乙巳", "value": "覆灯火" },
        { "key": "甲子", "value": "海中金" }
        // ... 60甲子完整映射
      ],
      "month_guaqi": [
        { "key": "01", "value": "艮土" },
        { "key": "11", "value": "坎水" }
      ]
    }
  }
}
```

### 类型定义

```typescript
export interface KeyValue {
  key: string;
  value: string;
}

export interface GuaTransformItem {
  code: string;
  name: string;
  wrong: string;      // 错卦
  overturn: string;   // 综卦
  mutual: string[];   // 互卦
}

export interface CalendarMaps {
  year_ganzhi: KeyValue[];
  ganzhi_nayin: KeyValue[];
  month_guaqi: KeyValue[];
}

export interface RulesMaps {
  gua_transform: GuaTransformItem[];
  calendar: CalendarMaps;
}

export interface RulesSchema {
  version: string;
  cards: RuleCard[];
  combos: RuleComboSummary[];
  maps: RulesMaps;
}
```

### 规则加载（懒加载）

```typescript
private loadRules(kb: KnowledgeBase): void {
  if (this.rulesLoaded) { return; }
  
  const items = kb.getAll();
  let text = '';
  for (let i = 0; i < items.length; i++) {
    if (items[i].id === 'rules/combination_rules.json' && items[i].type === 'json') {
      text = items[i].content;
      break;
    }
  }
  
  if (text === '') { this.rulesLoaded = true; return; }
  
  // 解析为固定结构（避免使用 any/unknown）
  const parsed = JSON.parse(text) as RulesSchema;
  this.ruleCards = parsed.cards;
  this.ruleCombos = parsed.combos;
  this.mapGuaTransform = parsed.maps.gua_transform;
  this.mapYearGanzhi = parsed.maps.calendar.year_ganzhi;
  this.mapGanzhiNayin = parsed.maps.calendar.ganzhi_nayin;
  this.mapMonthGuaqi = parsed.maps.calendar.month_guaqi;
  this.rulesLoaded = true;
}
```

### 六十四卦错/综/互计算

```typescript
// 遍历识别到的八卦
for (let bi = 0; bi < baguaFound.length; bi++) {
  const name = baguaFound[bi];
  // 从映射表中查找
  for (let gi = 0; gi < this.mapGuaTransform.length; gi++) {
    const gt = this.mapGuaTransform[gi];
    if (gt.name === name) {
      summaryLines.push('· 六十四卦变换: ' + name + ' → 错卦 ' + gt.wrong 
                        + '，综卦 ' + gt.overturn 
                        + (gt.mutual.length > 0 ? ('，互卦 ' + gt.mutual.join('、')) : ''));
      break;
    }
  }
}
```

### 万年历日期→干支/纳音/卦气

```typescript
// 第一步：正则提取日期
const dateRegex = /\d{4}-\d{2}-\d{2}/;
const dm = dateRegex.exec(query);  // "2025-11-22"

if (dm) {
  const dateStr = dm[0];
  const yearStr = dateStr.substring(0, 4);   // "2025"
  const monthStr = dateStr.substring(5, 7);  // "11"
  
  // 第二步：年份→干支（优先映射，回退算法）
  const ganzhi = this.calcYearGanzhi(yearStr);
  
  // 第三步：干支→纳音（直接映射）
  const nayin = this.findKV(this.mapGanzhiNayin, ganzhi);
  
  // 第四步：月份→卦气（直接映射）
  const guaqi = this.findKV(this.mapMonthGuaqi, monthStr);
  
  summaryLines.push('· 日期计算: ' + dateStr + ' → 年干支 ' + ganzhi 
                    + (nayin !== '' ? ('，纳音 ' + nayin) : '') 
                    + (guaqi !== '' ? ('，卦气 ' + guaqi) : ''));
}
```

#### 年份→干支算法（映射缺失时回退）

```typescript
private calcYearGanzhi(yearStr: string): string {
  // 优先从映射表查找
  const m = this.findKV(this.mapYearGanzhi, yearStr);
  if (m !== '') { return m; }
  
  // 回退算法：以 1984 年甲子为基准
  const year = parseInt(yearStr);
  const base = 1984;  // 甲子年
  const offset = year - base;
  
  const ganArr = ['甲','乙','丙','丁','戊','己','庚','辛','壬','癸'];
  const zhiArr = ['子','丑','寅','卯','辰','巳','午','未','申','酉','戌','亥'];
  
  let idxGan = offset % 10;   // 天干循环
  let idxZhi = offset % 12;   // 地支循环
  
  if (idxGan < 0) { idxGan += 10; }
  if (idxZhi < 0) { idxZhi += 12; }
  
  return ganArr[idxGan] + zhiArr[idxZhi];
}

private findKV(arr: KeyValue[], key: string): string {
  for (let i = 0; i < arr.length; i++) {
    if (arr[i].key === key) { return arr[i].value; }
  }
  return '';
}
```

---

## 性能优化策略

### 1. 索引优先（O(1) vs O(n)）

```typescript
// ✅ 好：Map 查找 O(1)
const ids = this.tokenIndex.get(token);

// ❌ 差：线性扫描 O(n)
for (let i = 0; i < items.length; i++) {
  if (items[i].title.indexOf(token) >= 0) { ... }
}
```

### 2. 分层回退（逐步降级）

```
索引命中（最快） → 扫描匹配（中等） → 二字分词（最慢）
```

仅在上一层未命中时才进入下一层。

### 3. 懒加载规则

```typescript
private loadRules(kb: KnowledgeBase): void {
  if (this.rulesLoaded) { return; }  // 仅加载一次
  // ... 加载规则
  this.rulesLoaded = true;
}
```

### 4. 去重逻辑

```typescript
const idSet: Set<string> = new Set();  // 利用 Set 自动去重
for (token of tokens) {
  const ids = this.tokenIndex.get(token);
  if (ids) {
    for (id of ids) {
      idSet.add(id);  // 重复 id 自动忽略
    }
  }
}
```

### 5. 中文优化

二字分词提升"洛书是啥"类自然语句命中率：

```
"洛书是啥" → ["洛书", "书是", "是啥"]
```

标题包含"洛书"的条目会被命中。

### 6. 异常保护

```typescript
// 面板搜索流程
try {
  const report = this.analyzer.analyze(trimmed, this.kb);
  out.push(report);
  this.bannerHint = this.analyzer.getHintFor(trimmed);
} catch (e) {
  this.bannerHint = '解析异常，已切换为卡片查询，请稍后重试';
}
```

### 7. 内存安全

- 规则文件体积控制（60甲子纳音 < 10KB）
- 六十四卦变换采用轻量映射（示例4条，可扩展）
- 按需加载 JSON，避免一次性加载全部数据

---

## 使用示例

### 示例1：输入"子丑寅卯"

```
1. 分词 → ["子丑寅卯"]（无分隔符，整体保留）+ 二字分词：["子丑","丑寅","寅卯"]
2. 索引命中 → dizhi_zi.json, dizhi_chou.json, dizhi_yin.json, dizhi_mao.json
   （因为这些文件的标签包含："子"/"丑"/"寅"/"卯"）
3. 组合识别 → zhiFound = ["子","丑","寅","卯"]
4. 关系计算 → 六合：子丑（命中）、寅亥（未命中亥）、卯戌（未命中戌）
5. 生成报告：
   【组合解析报告】
   · 地支识别: 子 丑 寅 卯
     六合: 子丑
6. 卡片结果：
   - 地支 · dizhi_zi (标签: 地支、十二地支、子、干支)
   - 地支 · dizhi_chou (标签: 地支、十二地支、丑、干支)
   - 地支 · dizhi_yin (标签: 地支、十二地支、寅、干支)
   - 地支 · dizhi_mao (标签: 地支、十二地支、卯、干支)
```

### 示例2：输入"河图 与 洛书"

```
1. 分词 → ["河图", "洛书"]（"与"被替换为空格）
2. 索引命中 → 
   - 标题包含"河图"的外部文档（doc_hetu）
   - 标题包含"洛书"的外部文档（doc_luoshu）
3. 组合识别 → zhiFound = [], ganFound = [], baguaFound = []（无符号）
4. 生成报告：
   【组合解析报告】
   · 关联提示: 六十四卦、焦氏易林、万年历等详解请参考本地知识库命中的卡片。
5. 卡片结果：
   - 河图简介 (标签: 河图、五行、数理、先天、生成)
   - 洛书简介 (标签: 洛书、九宫、后天、方位、数理)
```

### 示例3：输入"日期 2025-11-22"

```
1. 分词 → ["日期", "2025-11-22"]
2. 正则匹配 → /\d{4}-\d{2}-\d{2}/ 提取 "2025-11-22"
3. 年份→干支：
   - 查映射表：year_ganzhi[{"key":"2025","value":"乙巳"}] → "乙巳"
4. 干支→纳音：
   - 查映射表：ganzhi_nayin[{"key":"乙巳","value":"覆灯火"}] → "覆灯火"
5. 月份→卦气：
   - 查映射表：month_guaqi[{"key":"11","value":"坎水"}] → "坎水"
6. 生成报告：
   【组合解析报告】
   · 日期计算: 2025-11-22 → 年干支 乙巳，纳音 覆灯火，卦气 坎水
7. 卡片结果：
   - （如有匹配"2025"/"11月"/"乙巳"的知识库条目，一并列出）
```

### 示例4：输入"离"

```
1. 分词 → ["离"]
2. 索引命中 → 
   - gua_30_li.json（标签可能包含"离"）
   - bagua_li.json（标签包含"离"）
3. 组合识别 → baguaFound = ["离"]
4. 八卦属性：
   · 八卦识别: 离
     离：火，南方，九宫数 9。
5. 六十四卦变换（如映射表中有"离"）：
   · 六十四卦变换: 离 → 错卦 坎，综卦 离，互卦 离
6. 卡片结果：
   - 六十四卦 · gua_30_li (标签: 离卦、火...)
   - 八卦 · bagua_li (标签: 离、火...)
```

---

## 技术栈

- **语言**: TypeScript (ArkTS)
- **平台**: HarmonyOS
- **数据结构**: Map<string, string[]>, Set<string>, KBItem[]
- **规则引擎**: JSON 配置 + 动态映射
- **时间复杂度**: 
  - 索引命中: O(1)
  - 扫描匹配: O(n)
  - 二字分词: O(n × m)（n=items数，m=分词数）
- **空间复杂度**: O(n)（n=知识库条目数）

---

## 未来优化方向

1. **补齐64卦错/综/互映射表**（当前仅示例4条）
2. **扩展60甲子纳音全表到规则引擎**（当前已完成）
3. **引入分词权重**（TF-IDF）提升相关性排序
4. **缓存热点查询结果**（LRU Cache）
5. **联网AI增强**（可选，需用户授权）

---

**文档版本**: 1.0.0  
**最后更新**: 2025-11-22  
**维护者**: agent 分支开发团队
